---
title: Shader Ripple
description: A full-screen animated background built with Three.js shaders. Renders smooth colorful line effects using a custom fragment shader.
component: true
---

<ComponentPreview
  name="shader-ripple-01"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[90%] **:[.preview]:min-h-[630px]"
  description="Full-screen animated shader background using Three.js."
  align="start"
/>

<ComponentPreview
  name="shader-ripple-02"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[90%] **:[.preview]:min-h-[630px]"
  description="Full-screen animated shader background using Three.js."
  align="start"
/>

<ComponentPreview
  name="shader-ripple-03"
  className="[&_.preview>[data-orientation=vertical]]:sm:max-w-[90%] **:[.preview]:min-h-[630px]"
  description="Full-screen animated shader background using Three.js."
  align="start"
/>

## Installation

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">CLI</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://aliimam.in/r/shader-ripple.json
```

</TabsContent> <TabsContent value="manual"> <Steps>

<Step>Copy and paste the following code into your project.</Step>

<ComponentSource name="shader-ripple" title="components/ui/shader-ripple.tsx" />

<Step>Update the import paths to match your project setup.</Step>

</Steps> </TabsContent> </CodeTabs>

## Usage

```tsx  
import { ShaderRipple } from "@/components/ui/shader-ripple"
```

```tsx  
export default function Component() {
  return (
    <main className="flex h-full w-full items-center justify-center overflow-hidden rounded-md border">
      <ShaderRipple
        loopDuration={0.9}
        waveIntensity={0.2}
        timeScale={1}
        backgroundColor="#db2777"
        color1="#c2410c"
        color3="#db2777"
        color2="#dc2626"
        rotation={45}
        mod={0.4}
      />
    </main>
  );
}
```
 