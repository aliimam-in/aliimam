import { PixelGrid } from "@/registry/default/components/pixelated-grid";

export default function Component() {
  return (
    <div className="flex h-100 w-full overflow-hidden items-center justify-center">
      <PixelGrid />
    </div>
  );
}
